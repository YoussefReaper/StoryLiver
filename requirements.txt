fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27
pydantic>=2.6
redis>=5.0
websockets>=12
anthropic>=0.40
# Required by FastAPI for the player-art upload endpoint (multipart forms).
python-multipart>=0.0.9
# Argon2id password hashing for accounts (OWASP first recommendation).
argon2-cffi>=23.1
# Optional durable-storage backup/restore on free hosts with no disk.
pymongo>=4.6
