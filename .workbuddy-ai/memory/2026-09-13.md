# 2026-09-13 — StoryLiver: the playthrough, played all the way through

Continues the session that started 2026-09-12 (see that file for the premise
parser and companion fixes). Both user-supplied OpenAI keys are dead — see the
12th. Everything below ran offline, with no key and no spend.

## The playthrough exists and converges
`tools/play.py` drives the real `engine.take_turn` for the user's exact premise
(`I and my girlfriend charlie (from hazbin hotel) in Demon Slayer verse`) and
writes `playtest.md`. **6 actions, 27 model calls, 0 unanswered.** The world is
`Kirisawa` — authored offline because the builder's own structure call is a
stub in mock mode, so the default build is a generic town with Demon Slayer
characters standing in it.

Everything authored lives in `spool/responses/` (gitignored): world structure,
laws, chapters, canon persona cards (memories + signature lines), the crossover
friction reactions, six World Master verdicts, six narrator passages.

## Why it took six passes to get there, and the fix
Hashing the whole prompt to key a spooled answer **does not converge**: author
the World Master's verdict for turn 1 and the narrator's prompt for turn 1
changes, so its answer is attached to a prompt that no longer exists — and so is
every answer after it. One turn resolved per pass.

`STORYLIVER_SPOOL_BY_ACTION=1` keys an answer on the player's own words instead
(the action the prompt carries). An action does not change when the world or a
persona does, so a whole run is authored in one pass. This is the single most
important thing to know about the harness.

Second cascade source: authoring the persona cards changed every anchor block,
which re-keyed every World Master prompt. Author the world before the turns.

## Two more real bugs fixed
- **The persona call got the raw premise as its SOURCE** — `SOURCE: I and my
  girlfriend charlie (from hazbin hotel) in Demon Slayer verse` for Tanjiro
  Kamado. It now resolves the host setting the way the narrator's source line
  already did (`worldforge.bootstrap`).
- **The harness printed every action twice** — the engine already renders the
  player's action as the turn's first feed entry, and `tools/play.py` printed it
  again. Also gave fate/beat/npc entries their own labels.

## Things observed while playing, NOT fixed
- **Nezuko is scheduled independently of Tanjiro.** Canon says she travels in
  the box on his back; the engine puts her in the square at midday with her
  muzzle on, in the sun, which her own CONSTRAINTS forbid ("Cannot stand
  sunlight"). Nothing links a character's schedule to their carrier's.
- **Fate firing early is correct, not a bug.** `_fate_ready` lets an event
  arrive early when the player is standing where it happens
  (`turn >= max(2, f["turn"] - 3)`). My turn-4 prose re-announced the well
  because I had not accounted for it; fixed in the passage, not the engine.
- **`friction` still has no floor when the model answers.** The code floor I
  added covers a failed call; a *thin* call is still a thin call.

## State
`1183 checks across 14 suites, 0 failures` before this session's last two
edits; re-verified after (see the log below).

## Git
`friday-handoff-fixes` is **4 commits ahead of origin** and unpushed. A
mis-labelled commit (`4a2c3e9 "UI update"`, unpushed) was split into the harness
commit + the UI commit. `prompts/`, `spool/`, `playtest.md`, `StoryLiver.zip`
are gitignored now. **Pushing is the user's call — not done.**
